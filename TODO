## TODOs

* server setup
** support multiple clients, e.g. laptop and phone

* client setup
** add files to sane default location, e.g. /etc/openvpn
** try to use `synchronize` module for syncing hosts
** ensure client packages are installed, if necessary
** create network manager connection
** activate network manager connection
